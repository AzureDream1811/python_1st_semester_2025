# ===========================================
# CORE REQUIREMENTS (Required for project)
# ===========================================
# Core requirements that work on all platforms including Windows
# Install with: pip install -r requirements.txt

# Django Framework
Django==4.2.7

# Environment variables
python-decouple==3.8

# Forms
django-crispy-forms==2.1
crispy-bootstrap4==2022.1
django-widget-tweaks==1.5.0

# Image handling
Pillow==10.1.0

# Security
django-cors-headers==4.3.0

# Development
django-debug-toolbar==4.2.0

# Testing
pytest==7.4.3
pytest-django==4.7.0

# Other utilities
python-slugify==8.0.1

# ===========================================
# OPTIONAL: MySQL Database
# ===========================================
# Note: mysqlclient requires MySQL client libraries and Visual C++ Build Tools
# Project works with SQLite by default. Uncomment if using MySQL:
# On Windows, you may need to:
# 1. Install MySQL Server or MySQL Connector/C
# 2. Install Visual C++ Build Tools
#
# Alternative: Use PyMySQL instead (pure Python, easier to install)
# pip install pymysql
# Then add to settings.py:
# import pymysql
# pymysql.install_as_MySQLdb()
#
# mysqlclient==2.2.0

# ===========================================
# OPTIONAL: AI/ML Libraries
# ===========================================
# These packages may be difficult to install on Windows
# Note: Project uses rule-based sentiment analysis as fallback
# Install these if you want advanced ML features:
#
# Note: fasttext requires Visual C++ Build Tools on Windows
# You may need to install from: https://visualstudio.microsoft.com/downloads/
# If fasttext fails, try installing pybind11 first:
# pip install pybind11
# pip install fasttext
#
# fasttext==0.9.2
# underthesea==6.8.0
# pandas==2.1.3
# numpy==1.26.2

